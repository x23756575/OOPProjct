package MainApp;
/**
 *
 * @author Blesson/Saboteur
 */

import javax.swing.*;
import java.awt.*;
import java.util.Map;

public class InfoForm extends JPanel {
//    private JLabel infoLabel;
//    private Map<String, String> topics;
//    private JButton topicButton;

 //   public InfoForm(Map<String, String> topics) {
 //       this.topics = topics;  // Pass topics from Content to App
 //       setLayout(new BorderLayout());

 //       // Label to show instructions
  //      infoLabel = new JLabel("Select a topic to learn more.", SwingConstants.CENTER);
  //      infoLabel.setFont(new Font("Arial", Font.BOLD, 16));
 //       add(infoLabel, BorderLayout.CENTER);

        // Panel to hold dynamically generated buttons
   //     JPanel buttonPanel = new JPanel(new GridLayout(0, 1)); // Vertical buttons
    //    for (String topic : topics.keySet()) {
   //         topicButton = new JButton(topic);
   //         topicButton.addActionListener(e -> showTopicInfo(topic));
   //         buttonPanel.add(topicButton);
   //     }
  //      add(buttonPanel, BorderLayout.SOUTH);
  //  }

  //  private void showTopicInfo(String topic) {
        // You can customize this logic to show detailed info for the selected topic
   //     String infoText = InfoContent.getTopicInfo(topic);
  //      infoLabel.setText(infoText);
  //  }
}
//END OF CODE